<% if user.avatar.attached? %>
  <%= image_tag user.avatar, class: "h-8 w-8 rounded-full object-cover", alt: user.username %>
<% else %>
  <%= image_tag user.avatar_thumbnail, class: "h-8 w-8 rounded-full object-cover", alt: user.username %>
<% end %>
