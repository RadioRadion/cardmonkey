<div class="col-5">
    <h2><%= locals[:title] %> </h2>
    <% if locals[:total_price] %>
      <h3>total : <%= locals[:total_price] %> €</h3>
    <% end %>
    <% locals[:cards].each do |card| %>
    <div class="card-target card-product" data-id=<%= card.id%> >
      <%= image_tag ("cards/#{card.image[:api_id]}.jpg") %>
      <div class="card-product-infos">
        <h2><%= card.name %></h2>
        <h2><%= card.image.price %></h2>
      </div>
    </div>
    <% end %>
  </div>
