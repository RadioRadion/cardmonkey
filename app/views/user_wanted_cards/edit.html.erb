<h1 class="text-xl font-bold text-center mb-6">Modifier la carte recherchée</h1>

<%= render 'form', 
           form_url: user_user_wanted_card_path(@user, @user_wanted_card),
           editing: true %>
