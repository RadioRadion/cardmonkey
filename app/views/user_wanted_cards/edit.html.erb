<div class="py-8">
  <div class="w-full max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-6">
      <span class="ap-label">Recherches</span>
      <h1 class="text-2xl font-bold tracking-tight mt-1">Modifier la recherche</h1>
    </div>

    <%= render 'form',
               form_url: user_user_wanted_card_path(@user, @user_wanted_card),
               editing: true %>
  </div>
</div>
