<div class="cm-theme min-h-screen py-8">
  <div class="w-full max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-6">
      <span class="cm-kicker text-cm-accent">Collection</span>
      <h1 class="cm-display text-4xl mt-1">Modifier la carte</h1>
    </div>

    <%= render 'form',
               form_url: user_user_card_path(@user, @user_card),
               editing: true %>
  </div>
</div>
