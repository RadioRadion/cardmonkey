<%# app/views/users/edit.html.erb %>
<div class="container mx-auto px-4 py-8">
  <div class="max-w-3xl mx-auto">
    <div class="bg-white shadow-lg rounded-lg overflow-hidden">
      <!-- Profile Header -->
      <div class="bg-gradient-to-r from-blue-500 to-blue-600 px-6 py-8">
        <div class="flex items-center space-x-4">
          <%= turbo_frame_tag "profile_avatar" do %>
            <%= render "users/avatar", user: @user %>
          <% end %>
          
          <%= turbo_frame_tag "profile_info" do %>
            <%= render "users/profile_info", user: @user, editing: @editing %>
          <% end %>
        </div>
      </div>

      <%= turbo_frame_tag "trading_preferences" do %>
        <%= render "users/trading_preferences", user: @user, editing_preferences: @editing_preferences %>
      <% end %>

      <!-- Trading Statistics -->
      <div class="p-6">
        <h2 class="text-lg font-medium text-gray-900 mb-4"><%= t('users.profile.trading_statistics') %></h2>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
          <div class="bg-gray-50 p-4 rounded-lg text-center transition-all duration-200 hover:shadow-md">
            <div class="text-2xl font-bold text-blue-600"><%= @stats[:total_matches] %></div>
            <div class="text-sm text-gray-500"><%= t('users.profile.total_matches') %></div>
          </div>
          <div class="bg-gray-50 p-4 rounded-lg text-center">
            <div class="text-2xl font-bold text-blue-600"><%= @stats[:unique_matched_users] %></div>
            <div class="text-sm text-gray-500"><%= t('users.profile.trading_partners') %></div>
          </div>
          <div class="bg-gray-50 p-4 rounded-lg text-center">
            <div class="text-2xl font-bold text-blue-600"><%= @user.user_cards.count %></div>
            <div class="text-sm text-gray-500"><%= t('users.profile.cards_listed') %></div>
          </div>
          <div class="bg-gray-50 p-4 rounded-lg text-center">
            <div class="text-2xl font-bold text-blue-600"><%= @user.user_wanted_cards.count %></div>
            <div class="text-sm text-gray-500"><%= t('users.profile.cards_wanted') %></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>